server:
  port: 8081

#对端口号的引用
users:
  customer:
    url: localhost:${server.port}
#spring:
#  datasource:
#    driver-class-name:
#      url: jdbc://192.168.1.100/test

teacher:
  id: ${random.uuid}
  age: ${random.int(30,50)}
  num1: ${random.int}
  numlong1: ${random.long}
  numlong2: ${random.long(10,20)}
  value: ${random.value}